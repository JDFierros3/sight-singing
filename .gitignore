# Node modules (only needed to run the OpenPsalm build script)
node_modules/

# Local Claude Code settings — machine-specific, not shared
.claude/settings.local.json

# Editor / tooling
.cursor/

# OS files
.DS_Store
Thumbs.db
